import { css } from 'lit';

const brand = css `#ff3e00`;

export const title = css`
    color: ${brand};
    text-transform: uppercase;
    font-size: 4em;
    font-weight: 100;
`;

export const header = css`
    color: ${brand};
    text-transform: uppercase;
    font-size: 4em;
    font-weight: 100;
`;